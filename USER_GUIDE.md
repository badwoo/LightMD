# LightMD 用户使用文档

## 简介

LightMD 是一款轻量级的 Windows Markdown 编辑器，支持**所见即所得（WYSIWYG）**编辑体验。在光标所在行，您可以看见 Markdown 语法标记（如 `#`、`>`、` ``` `），而其他行则渲染为漂亮的富文本格式。

## 安装与启动

### 运行方式

直接双击 `lightmd.exe` 启动应用。无需安装，绿色运行。

### 系统要求

- Windows 10/11（64 位）
- 无需安装额外运行库

## 基本操作

### 文件管理

| 操作 | 快捷键 | 说明 |
|------|--------|------|
| 打开文件夹 | 侧边栏 📂 按钮 | 选择文件夹后，侧边栏显示文件树 |
| 打开文件 | `Ctrl+O` 或点击文件 | 支持 .md / .markdown / .mdown / .txt |
| 新建文件 | 侧边栏 📄 按钮 或 `Ctrl+N` | 在已打开文件夹的根目录创建 |
| 新建文件夹 | 侧边栏 📁 按钮 | 右键文件夹也有此选项 |
| 保存文件 | `Ctrl+S` | 若从未保存过，调用「另存为」 |
| 另存为 | `Ctrl+Shift+S` | 选择保存位置 |
| 拖拽打开 | 拖拽 .md 文件到编辑器 | 自动加载内容 |
| 重命名 | 右键文件 → 重命名 | 支持内联编辑 |
| 删除 | 右键文件 → 删除 | 有确认对话框 |

### 内容编辑

LightMD 使用 **Markdown 实时预览**模式。输入 Markdown 语法即可看到渲染效果。

#### 支持的语法

| 语法 | 输入 | 效果 |
|------|------|------|
| 标题 | `#` ~ `###### ` + 空格 | 六级标题 |
| 加粗 | `**文本**` 或 `Ctrl+B` | **加粗文本** |
| 斜体 | `*文本*` 或 `Ctrl+I` | *斜体文本* |
| 删除线 | `~~文本~~` | ~~删除线文本~~ |
| 行内代码 | `` `代码` `` 或 `Ctrl+\`` | `行内代码` |
| 代码块 | ` ```语言 ` + 回车 | 带语法高亮的代码块 |
| 引用 | `> ` + 空格 | 引用块 |
| 无序列表 | `- ` 或 `* ` 或 `+ ` + 空格 | 无序列表 |
| 有序列表 | `1. ` + 空格 | 有序列表 |
| 分割线 | `---` 或 `***` + 回车 | 水平分割线 |
| 链接 | `[文本](URL)` | 超链接 |
| 图片 | `![描述](URL)` 或直接粘贴图片 | 图片嵌入 |
| 表格 | 直接粘贴 Markdown 表格 | 可视化表格 |

#### 列表操作

| 操作 | 快捷键 |
|------|--------|
| 增加缩进 | `Tab` |
| 减少缩进 | `Shift+Tab` |
| 列表项换行 | `Enter` |

### 图片粘贴

1. 复制图片到剪贴板，在编辑器中 `Ctrl+V` 粘贴
2. 或直接拖拽图片文件到编辑器
3. 弹窗提供两种处理方式：
   - **Base64 内联**：图片以 Base64 编码嵌入 Markdown，适合小图片
   - **保存到 assets/**：图片保存到文档目录的 `assets/` 文件夹，Markdown 使用相对路径引用

### 导出

| 格式 | 快捷键 | 说明 |
|------|--------|------|
| HTML | `Ctrl+Shift+E` → HTML | 可选的样式内嵌 |
| PDF | `Ctrl+Shift+E` → PDF | 通过浏览器打印保存为 PDF |

## 视图功能

### 主题切换

- `Ctrl+Shift+T` 切换亮色/暗色主题
- 设置面板中也可切换

### 大纲视图

- 右侧面板显示文档标题结构
- 点击标题跳转到对应位置
- `Ctrl+Shift+O` 切换显示/隐藏

### 专注模式

- `F8` 开关专注模式
- 当前编辑段落高亮，其余段落变暗

### 打字机模式

- 设置面板中开启
- 编辑时光标始终保持在屏幕中间

## 设置面板

`Ctrl+,` 打开设置面板：

| 设置项 | 说明 |
|--------|------|
| 主题 | 亮色/暗色 |
| 字体大小 | 12px ~ 28px |
| 字体 | 系统默认/微软雅黑/苹方/Consolas/Fira Code/Cascadia Code |
| 自动保存 | 0~60 秒，0 = 关闭 |

## 快捷键速查

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+O` | 打开文件 |
| `Ctrl+S` | 保存文件 |
| `Ctrl+Shift+S` | 另存为 |
| `Ctrl+N` | 新建文件 |
| `Ctrl+B` | 加粗 |
| `Ctrl+I` | 斜体 |
| `` Ctrl+` `` | 行内代码 |
| `Ctrl+1~6` | 设置标题级别 |
| `Ctrl+0` | 清除标题（转为段落） |
| `Ctrl+Z` | 撤销 |
| `Ctrl+Y` / `Ctrl+Shift+Z` | 重做 |
| `Tab` | 列表缩进 |
| `Shift+Tab` | 列表减少缩进 |
| `Ctrl+Shift+8` | 无序列表 |
| `Ctrl+Shift+9` | 有序列表 |
| `Ctrl+Shift+.` | 引用块 |
| `Ctrl+Shift+T` | 切换主题 |
| `Ctrl+Shift+E` | 导出 |
| `Ctrl+Shift+O` | 切换大纲 |
| `Ctrl+,` | 设置 |
| `F8` | 专注模式 |

## 常见问题

### Q: 为什么点击「打开文件夹」没有反应？
A: 请确认选择的文件夹有读取权限。系统保护目录（如 `C:\Windows`）可能无法正常读取。

### Q: 编辑器没有显示 Markdown 渲染？
A: LightMD 使用「即时渲染」模式——只有在**光标不在的行**才会渲染。光标所在行会显示 Markdown 源码，离开该行后渲染生效。

### Q: 保存文件后，编辑内容丢失了？
A: 请检查是否以「另存为」方式保存到了正确位置。内容同时会缓存在本地，重启后可恢复。

### Q: 如何重置所有设置？
A: 清除浏览器 localStorage 中的 `lightmd-settings` 键。未来版本将通过 SQLite 管理设置。
