# LightMD 用户使用文档

> 适用于 v0.2.0 及以上版本

## 简介

LightMD 是一款轻量级的 Windows Markdown 编辑器，支持**所见即所得（WYSIWYG）**编辑体验。在光标所在行，您可以看见 Markdown 语法标记（如 `#`、`>`、```````），而其他行则渲染为漂亮的富文本格式。

## 安装与启动

### 运行方式

直接双击 `lightmd.exe` 启动应用。无需安装，绿色运行。

### 系统要求

- Windows 10/11（64 位）
- 无需安装额外运行库

## 基本操作

### 文件管理
| 操作 | 快捷键 | 说明 |
| --- | --- | --- |
| 打开文件夹 | 侧边栏 📂 按钮 | 选择文件夹后，侧边栏显示文件树 |
| 打开文件 | `Ctrl+O` 或点击文件 | 支持 .md / .markdown / .mdown / .txt |
| 新建文件 | 侧边栏 📄 按钮 或 `Ctrl+N` | 在已打开文件夹的根目录创建 |
| 新建文件夹 | 侧边栏 📁 按钮 | 右键文件夹也有此选项 |
| 保存文件 | `Ctrl+S` | 若从未保存过，调用「另存为」 |
| 另存为 | `Ctrl+Shift+S` | 选择保存位置 |
| 拖拽打开 | 拖拽 .md 文件到编辑器 | 自动加载内容 |
| 重命名 | 右键文件 → 重命名 | 支持内联编辑 |
| 删除 | 右键文件 → 删除 | 有确认对话框 |

### 内容编辑

LightMD 使用 **Markdown 实时阅读**模式。输入 Markdown 语法即可看到渲染效果。

#### 支持的语法
| 语法 | 输入 | 效果 |
| --- | --- | --- |
| 标题 | `#` ~ `###### ` + 空格 | 六级标题 |
| 加粗 | `**文本**` 或 `Ctrl+B` | **加粗文本** |
| 斜体 | `*文本*` 或 `Ctrl+I` | *斜体文本* |
| 删除线 | `~~文本~~` | ~~删除线文本~~ |
| 行内代码 | ```代码``` 或 `Ctrl+`` | `行内代码` |
| 代码块 | `````语言`` + 回车 | 带语法高亮的代码块 |
| 引用 | `> ` + 空格 | 引用块 |
| 无序列表 | `- ` 或 `* ` 或 `+ ` + 空格 | 无序列表 |
| 有序列表 | `1. ` + 空格 | 有序列表 |
| 分割线 | `---` 或 `***` + 回车 | 水平分割线 |
| 链接 | `[文本](URL)` | 超链接 |
| 图片 | `![描述](URL)` 或直接粘贴图片 | 图片嵌入 |
| 表格 | 直接粘贴 Markdown 表格 | 可视化表格 |

#### 0.2.0 新增 Markdown 格式
| 语法 | 输入 | 效果 |
| --- | --- | --- |
| 高亮标记 | `==文本==` | <mark>高亮文本</mark> |
| 上标 | `^文本^` | 文本^上标^ |
| 下标 | `~文本~` | 文本~下标~ |
| Emoji 表情 | `:smile:` `:heart:` `:+1:` | 😄 ❤️ 👍（输入冒号后自动补全） |
| 脚注 | `[^1]` + 文末 `[^1]: 说明` | 文末自动汇总脚注 |
| 定义列表 | `术语` 换行 `: 定义` | 术语 + 缩进定义 |
| 自动目录 | `[toc]` 或 `[[toc]]` | 根据标题层级自动生成目录 |
| 锚点链接 | `[链接](#标题文本)` | 跳转到文档内对应标题（标题自动生成 id） |

> 提示：锚点链接的 id 由标题文本自动生成（小写、空格替换为连字符）。例如标题 `## 快速上手` 的锚点为 `#快速上手`，链接写法为 `[跳转](#快速上手)`。

#### 列表操作
| 操作 | 快捷键 |
| --- | --- |
| 增加缩进 | `Tab` |
| 减少缩进 | `Shift+Tab` |
| 列表项换行 | `Enter` |

### 图片粘贴与插入

#### 粘贴图片

1. 复制图片到剪贴板，在编辑器中 `Ctrl+V` 粘贴
2. 或直接拖拽图片文件到编辑器
3. **Base64 内联**：图片以 Base64 编码嵌入 Markdown，适合小图片
4. **保存到 assets/**：图片保存到文档目录的 `assets/` 文件夹，Markdown 使用相对路径引用（0.2.0 真正实现）
5. **保存到 assets/**：图片保存到文档目录的 `assets/` 文件夹，Markdown 使用相对路径引用（0.2.0 真正实现）

#### 从文件选择插入图片（0.2.0 新增）

通过工具栏或右键菜单的「插入图片」按钮，可打开图片选择对话框：

1. 点击「选择文件」从本地选择图片
2. **Base64 内联**：图片以 Base64 编码直接嵌入
3. **保存到 assets/**：图片复制到文档同级 `assets/` 目录，使用相对路径引用
4. **保存到 assets/**：图片复制到文档同级 `assets/` 目录，使用相对路径引用
5. 确认后自动生成 Markdown 图片语法插入

### 导出
| 格式 | 快捷键 | 说明 |
| --- | --- | --- |
| HTML | `Ctrl+Shift+E` → HTML | 可选的样式内嵌 |
| PDF | `Ctrl+Shift+E` → PDF | 通过浏览器打印保存为 PDF |

## 视图功能

### 主题切换

- `Ctrl+Shift+T` 切换亮色/暗色主题
- 设置面板中也可切换

### 大纲视图

- 右侧面板显示文档标题结构
- 点击标题跳转到对应位置
- `Ctrl+Shift+O` 切换显示/隐藏

### 专注模式

- `F8` 开关专注模式
- 当前编辑段落高亮，其余段落变暗
- 与打字机模式可同时开启配合使用
- 在阅读/编辑/分屏三种模式下均可用

### 打字机模式

- `F9` 快捷键开关打字机模式
- 编辑时光标始终保持在屏幕中间
- 与专注模式可同时开启配合使用
- 在阅读/编辑/分屏三种模式下均可用

## Slash 命令（0.2.0 新增）

在**空行行首**输入 `/` 字符，即可触发 Slash 命令快速插入菜单。该菜单提供常用块级元素的快捷插入：
| 菜单项 | 说明 |
| --- | --- |
| 标题 H1 ~ H6 | 插入对应级别的标题 |
| 无序列表 / 有序列表 | 插入列表项 |
| 任务列表 | 插入 `- [ ]` 待办项 |
| 引用块 | 插入 `>` 引用 |
| 代码块 | 插入带语言标注的代码块 |
| 表格 | 调出表格插入对话框 |
| 水平分割线 | 插入 `---` |
| Mermaid 图表 | 调出 Mermaid 模板下拉 |
| 数学公式 | 插入块级 `$$...$$` 公式 |
| 自动目录 | 插入 `[toc]` |

用法：

1. 在新行行首输入 `/`
2. 通过方向键或鼠标选择菜单项
3. 按 `Enter` 确认插入；按 `Esc` 取消

> 注意：Slash 命令仅在行首（行内无其他字符）触发，避免与除号等符号冲突。

## 插入对话框（0.2.0 新增）

### 链接插入对话框

通过工具栏 🔗 按钮或右键菜单「插入链接」打开：
| 输入项 | 说明 |
| --- | --- |
| 链接文本 | 显示在 Markdown 中的文字 |
| URL | 链接地址（可填写 `#锚点` 实现页内跳转） |
| 标题（可选） | 鼠标悬停时的提示文字 |
| 实时预览 | 对话框底部实时显示生成的 `[文本](URL "标题")` 语法 |

### 表格插入对话框

通过工具栏 📊 按钮或右键菜单「插入表格」打开：
| 配置项 | 说明 |
| --- | --- |
| 行数 | 表格行数（含表头行） |
| 列数 | 表格列数 |
| 包含表头 | 勾选后首行作为表头（默认勾选） |

确认后自动生成对齐的 Markdown 表格语法。

### 图片插入对话框

见上文「从文件选择插入图片」章节。

## 右键菜单（0.2.0 新增）

### 编辑器右键菜单

在编辑器空白处右键，弹出以下操作：
| 菜单项 | 说明 |
| --- | --- |
| 撤销 / 重做 | 等同 `Ctrl+Z` / `Ctrl+Y` |
| 剪切 / 复制 / 粘贴 | 标准剪贴板操作 |
| 格式化 | 整理当前选区的 Markdown 格式 |
| 插入链接 | 打开链接插入对话框 |
| 插入表格 | 打开表格插入对话框 |
| 插入图片 | 打开图片选择对话框 |
| 插入代码块 | 插入 ``````` 代码块 |
| 插入 Mermaid | 打开 Mermaid 模板下拉 |

### 表格右键菜单（阅读模式）

在阅读模式下，将光标置于表格单元格内右键，可执行：
| 菜单项 | 说明 |
| --- | --- |
| 在上方插入行 | 当前行上方新增一行 |
| 在下方插入行 | 当前行下方新增一行 |
| 在左侧插入列 | 当前列左侧新增一列 |
| 在右侧插入列 | 当前列右侧新增一列 |
| 删除当前行 | 移除光标所在行 |
| 删除当前列 | 移除光标所在列 |

> 提示：表格右键菜单仅在阅读模式（ProseMirror 富文本）下可用，源码模式请直接编辑 Markdown 表格文本。

## Mermaid 模板（0.2.0 新增）

工具栏提供 Mermaid 模板下拉菜单，支持一键插入以下 7 种常用图表的代码骨架：
| 模板 | 说明 | 适用场景 |
| --- | --- | --- |
| Flowchart | 流程图 | 流程、决策分支 |
| Sequence | 时序图 | 对象间交互、消息时序 |
| State | 状态图 | 状态机、状态流转 |
| Gantt | 甘特图 | 项目计划、任务时间线 |
| Pie | 饼图 | 数据占比 |
| ER 图 | 实体关系图 | 数据库设计 |
| Gitgraph | Git 提交图 | 分支策略、版本演进 |

使用方法：

1. 点击工具栏 Mermaid 下拉按钮
2. 选择对应模板
3. 编辑器自动插入 `````mermaid`` 代码块，内含示例代码
4. 修改内容即可实时渲染

## 代码高亮（0.2.0 扩展）

LightMD 使用 **PrismJS** 进行代码语法高亮，支持 200+ 种语言。

### 语法辅助栏代码块模板

通过语法辅助栏可一键插入以下语言的代码块骨架（0.2.0 补全）：
| 模板 | 模板 | 模板 |
| --- | --- | --- |
| TypeScript | Go | Rust |
| Java | C++ | SQL |
| JSON | YAML | Bash |
| Markdown | PHP |   |

使用方法：点击语法辅助栏对应语言按钮，自动插入 `````语言`` 代码块。

### 0.2.0 新增高亮语言

PrismJS 在 0.2.0 版本新增以下 10 种语言的语法高亮支持：

- PHP
- Swift
- Kotlin
- Dart
- Lua
- Ruby
- R
- Scala
- Perl
- PowerShell

在代码块声明语言即可启用对应高亮，例如：

```
```kotlin
fun main() {
    println("Hello, LightMD")
}
```

```

## 设置面板

`Ctrl+,` 打开设置面板：
| 设置项 | 说明 |
| --- | --- |
| 主题 | 亮色/暗色 |
| 字体大小 | 12px ~ 28px |
| 字体 | 系统默认/微软雅黑/苹方/Consolas/Fira Code/Cascadia Code |
| 自动保存 | 0~60 秒，0 = 关闭 |

## 快捷键

### 文件与编辑
| 快捷键 | 功能 | 适用模式 |
| --- | --- | --- |
| `Ctrl+N` | 新建文件 | 全局 |
| `Ctrl+O` | 打开文件 | 全局 |
| `Ctrl+S` | 保存文件 | 全局 |
| `Ctrl+Shift+S` | 另存为 / 删除线 | 全局另存为；阅读模式下为删除线 |
| `Ctrl+Shift+E` | 导出 HTML | 全局 |
| `Ctrl+Z` | 撤销 | 全局 |
| `Ctrl+Y` / `Ctrl+Shift+Z` | 重做 | 全局 |
| `Ctrl+F` | 搜索 | 全局 |
| `Ctrl+H` | 查找替换 | 全局 |

### 格式快捷键（源码模式，0.2.0 新增）
| 快捷键 | 功能 |
| --- | --- |
| `Ctrl+B` | 加粗 `**文本**` |
| `Ctrl+I` | 斜体 `*文本*` |
| ``Ctrl+``` | 行内代码 ```代码``` |
| `Ctrl+Alt+S` | 删除线 `~~文本~~` |
| `Ctrl+Shift+M` | 块级公式 `$$...$$` |
| `Ctrl+1` ~ `Ctrl+6` | 设置标题级别 H1 ~ H6 |
| `Ctrl+0` | 移除标题（转为段落） |

### 格式快捷键（阅读模式 ProseMirror，0.2.0 新增）
| 快捷键 | 功能 |
| --- | --- |
| `Ctrl+Shift+S` | 删除线 |

### 列表快捷键
| 快捷键 | 功能 |
| --- | --- |
| `Tab` | 列表缩进 |
| `Shift+Tab` | 列表减少缩进 |
| `Ctrl+Shift+8` | 无序列表 |
| `Ctrl+Shift+9` | 有序列表 |
| `Ctrl+Shift+.` | 引用块 |

### 快速插入
| 快捷键 | 功能 |
| --- | --- |
| 行首 `/` | 触发 Slash 命令菜单（0.2.0 新增） |

### 视图与模式
| 快捷键 | 功能 |
| --- | --- |
| `Ctrl+Shift+T` | 切换主题 |
| `Ctrl+Shift+O` | 切换大纲 / 语法辅助 |
| `Ctrl+Shift+E` | 导出 |
| `Ctrl+,` | 设置 |
| `F8` | 专注模式 |
| `F9` | 打字机模式 |
| 双击 `Ctrl` | 切换阅读 / 编辑 |
| 双击 `Shift` | 切换分屏模式 |

### 标签页
| 快捷键 | 功能 |
| --- | --- |
| `Ctrl+W` | 关闭当前标签 |
| `Ctrl+Tab` | 切换到下一个标签 |
| `Ctrl+Shift+Tab` | 切换到上一个标签 |

## 常见问题

### Q: 为什么点击「打开文件夹」没有反应？

A: 请确认选择的文件夹有读取权限。系统保护目录（如 `C:\Windows`）可能无法正常读取。

### Q: 编辑器没有显示 Markdown 渲染？

A: LightMD 使用「即时渲染」模式——只有在**光标不在的行**才会渲染。光标所在行会显示 Markdown 源码，离开该行后渲染生效。

### Q: 保存文件后，编辑内容丢失了？

A: 请检查是否以「另存为」方式保存到了正确位置。内容同时会缓存在本地，重启后可恢复。

### Q: Slash 命令菜单没有出现？

A: Slash 命令仅在**空行行首**触发。请确认当前行除 `/` 外没有其他字符，且光标紧跟在 `/` 之后。

### Q: 表格右键菜单不可用？

A: 表格右键菜单仅在**阅读模式**下可用。请通过双击 `Ctrl` 切换到阅读模式，再将光标置于表格单元格内右键。

### Q: Emoji 缩写不知道怎么写？

A: Emoji 使用 GitHub 短码格式（如 `:smile:`、`:heart:`、`:+1:`）。输入 `:` 后会弹出自动补全列表，可通过方向键选择。

### Q: 锚点链接跳转失败？

A: 锚点 id 由标题文本自动生成，规则为：转小写、空格替换为连字符、移除特殊字符。请确保 `[链接](#锚点)` 中的锚点与目标标题生成的 id 一致。

### Q: 如何重置所有设置？

A: 清除浏览器 localStorage 中的 `lightmd-settings` 键。未来版本将通过 SQLite 管理设置。
